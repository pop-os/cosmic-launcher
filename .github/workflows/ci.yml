name: Continuous integration

on:
  push:
    branches: [master]
  pull_request:

env:
  CARGO_TERM_COLOR: always
  DEBIAN_FRONTEND: noninteractive
  # `tokio::task::Builder` is gated behind this, and we need it for the `console` feature of cosmic launcher
  RUSTFLAGS: --cfg tokio_unstable

jobs:
  fmt:
    name: Rustfmt
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5
      - name: install toolchain
        run: rustup toolchain install nightly --component rustfmt
      - name: fmt
        run: cargo +nightly fmt --all --check

  clippy:
    name: Clippy
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5
      - name: install system dependencies
        run: sudo apt-get update && sudo apt-get install -y pkg-config libxkbcommon-dev
      - name: install toolchain
        run: rustup show && rustup component add clippy
      - uses: Swatinem/rust-cache@v2
      - name: clippy
        run: cargo clippy --all-features --all-targets -- -D warnings

  test:
    name: Test
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5
      - name: install system dependencies
        run: sudo apt-get update && sudo apt-get install -y pkg-config libxkbcommon-dev
      - name: install toolchain
        run: rustup show
      - uses: Swatinem/rust-cache@v2
      - name: test
        run: cargo test --all-features
